PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d

# Default admin account (used by database/seed.sql — see that
# file for the corresponding bcrypt hash; this line is just
# documentation of the plaintext dev password)
ADMIN_EMAIL=admin@bloodcare.com
ADMIN_PASSWORD=admin123

# MySQL connection (Phase 3)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bloodcare
